// Feature flags management

export function useFeatureFlag(flagKey: string): boolean {
  // For now, return false for all feature flags
  // This can be enhanced to check against Convex database
  return false;
}
